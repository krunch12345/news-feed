Place TagType font files here to enable the header logo font.

Expected filenames (any one is enough):
- `TagType-Regular.woff2` (preferred)
- `TagType-Regular.woff`
- `TagType-Regular.ttf`

The font is referenced in `styles/globals.css` via `@font-face`.
